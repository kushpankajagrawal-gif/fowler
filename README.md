# Phoolfolk

A colourful flower-shop storefront built with React and Vite.

## Run locally

1. Install Node.js 18+.
2. Open a terminal in this folder.
3. Run `npm install`.
4. Run `npm run dev`.

The storefront includes category filtering, responsive navigation, product cards, and a functional shopping bag drawer with subtotal calculation.
